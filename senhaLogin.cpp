#include<stdio.h >
#include <stdlib.h>
#include<string.h>

main () {		
	
	int usernome = strlen(nome);
	int usersenha = strlen(senha);
	printf("Informe seu login: ");
	scanf ("%s", &login);
	
	printf("Informe sua senha: ");
	scanf ("%s", &senha);
	
	
	if("%s", login == "%s", senha){
		printf("Errado");
	}
	else{
		printf("Certo");
	}
}
